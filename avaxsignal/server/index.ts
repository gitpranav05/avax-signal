/**
 * server/index.ts — Express server entry point
 *
 * Implemented in Phase 3. See Phases.md.
 *
 * Responsibilities:
 *   - Express API routes (signals, trades, portfolio)
 *   - Socket.IO setup
 *   - Serves the React dashboard in production
 */

export {};
