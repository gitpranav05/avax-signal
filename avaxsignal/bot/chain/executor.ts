/**
 * executor.ts — Signs & sends testnet swap transactions
 *
 * Implemented in Phase 4. See Phases.md.
 *
 * Responsibilities:
 *   - Sign and send one real swap transaction to a DEX router on Fuji
 *   - Triggered manually from the dashboard
 *   - MUST log what will happen BEFORE signing/sending (Rules.md security #3)
 *   - TESTNET ONLY — never mainnet (Rules.md security #1)
 */

export {};
